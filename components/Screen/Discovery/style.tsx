import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex:1,
    },
    text: {
        alignSelf: 'center',
        position: 'absolute',
        top: '50%',
    }
});

export default styles;